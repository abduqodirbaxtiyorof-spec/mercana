import type { Config } from "tailwindcss";

/** Unused — MICASA uses /micasa/styles/main.css. Kept for tooling compatibility. */
const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
};

export default config;
