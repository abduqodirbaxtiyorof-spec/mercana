import type { Config } from "tailwindcss";

/** Unused — site uses /micasa/styles/main.css. Kept for tooling compatibility. */
const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
};

export default config;
